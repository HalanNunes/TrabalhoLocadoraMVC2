@model TrabalhoLocadoraMVC2.Models.Genero

@{
    ViewBag.Title = "Details";
}
<body style="padding-top: 40px; padding-bottom: 40px; background-color: #f5f5f5;">
    <div class="container">
        <div class="div-signin">
            <fieldset>
                <legend>Detalhes do Genero</legend>

                <div class="display-label">
                     @Html.DisplayNameFor(model => model.Descricao): 
                    @Html.DisplayFor(model => model.Descricao)
                </div>
            </fieldset>
            <br>
            <button class="btn btn-large btn-primary">@Html.ActionLink("Editar", "Edit", new { id=Model.Id })</button>
            <button class="btn btn-large btn-primary">@Html.ActionLink("Retornar à Lista", "Index")</button>
        </div>
    </div>
</body>